name: Tag
description: Introduces a Tag multiplayer minigame to the server. 
version: 1.1.0
main: org.aurora.tag.Tag
authors: ["RussianMushroom"]

commands:
  tag:
    description: 
  
permissions:
  tag.start:
    default: op
  tag.stop:
    default: op
  tag.join:
    default: op
  tag.help:
    default: op
  tag.leave:
    default: op
  tag.set:
    default: op
  tag.leaderboard:
    default: op
  tag.reward:
    default: op